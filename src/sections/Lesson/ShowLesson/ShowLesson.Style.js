import styled from "styled-components";

const ShowLessonStyleWrapper = styled.div`
  background: var(--primary-background-color);
  padding-top: 70px;
  padding-bottom: 140px;
  p {
    margin-bottom: 26px;
  }
`;

export default ShowLessonStyleWrapper;
